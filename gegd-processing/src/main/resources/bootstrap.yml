micronaut:
  application:
    name: gegd-processing
  config-client:
    enabled: true

kubernetes:
  client:
    secure: true
    namespace: gegd-camel

    secrets:
      enabled: false
    discovery:
      enabled: true

    config-maps:
      enabled: true
      watch: true
      labels:
        - app: gegd-processing