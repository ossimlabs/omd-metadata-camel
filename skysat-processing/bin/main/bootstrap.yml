micronaut:
  application:
    name: skysat-processing
  config-client:
    enabled: true

kubernetes:
  client:
    secure: true
    namespace: camel-caravan

    secrets:
      enabled: false
    discovery:
      enabled: true

    config-maps:
      enabled: true
      watch: true
      labels:
        - app: skysat-processing  